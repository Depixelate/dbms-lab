for 1-6, add 2 tuples to relations, then select *, then add 1 for every constraint violated.
for alter, desc table before, then execute alter, then desc table after
for increasing size of fields, show limit in prev command, then alter, then show higher limit now.
For NULL constraints, you should demonstrate them by adding rows with NULL, not using desc.
Desc table, Test all constraints for a table immediately after creating it.
Try to make rem statements match questions.
